#include "main.h"
/**
 * _abs - compute absolute value
 * @n: input number as integer
 * Return: absolute value of n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
